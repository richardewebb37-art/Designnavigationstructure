
  # Design Navigation Structure

  This is a code bundle for Design Navigation Structure. The original project is available at https://www.figma.com/design/KLIxQWlr9DNvr5zhy1MiBr/Design-Navigation-Structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  